[build]
builder = "nixpacks"
buildCommand = "cd scraper && npm install && npx playwright install chromium chrome --with-deps && npm run build"

[deploy]
startCommand = "node scraper/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "always"
