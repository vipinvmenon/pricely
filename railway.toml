[build]
builder = "nixpacks"
buildCommand = "cd scraper && npm install && npm run build"

[deploy]
startCommand = "cd scraper && node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "always"
