'use client'

/**
 * `Glass` is the legacy name for the canonical {@link GlassCard} primitive.
 * Prefer importing `GlassCard` in new code; this alias keeps existing call
 * sites working without churn.
 */
export { GlassCard, GlassCard as Glass, type GlassVariant } from './GlassCard'
