export default function SignInLoading() {
  return (
    <div
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        background: 'var(--bg0)',
        color: 'var(--text-dim)',
        fontFamily: 'var(--font-mono)',
        fontSize: '0.875rem',
      }}
    >
      Loading sign in…
    </div>
  )
}
